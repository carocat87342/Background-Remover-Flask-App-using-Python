{
    "build": {
        "cmd": "pip install -r requirements.txt && python -m venv env && source env/bin/activate && python -m pip install --upgrade pip && python -m pip install -r requirements.txt && python app.py",
        "env": {
            "PYTHON_VERSION": "3.10"
        }
    }
}
